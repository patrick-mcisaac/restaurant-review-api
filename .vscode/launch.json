{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Django",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/manage.py",
      "args": ["runserver", "--settings=restaurantproject.settings.base"],
      "django": true,
      "autoReload": {
        "enable": true
      }
    }
  ]
}
